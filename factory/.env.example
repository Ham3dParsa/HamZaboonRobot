# HamZaban lexicon factory — research API keys (TEMPLATE, committed).
# Real values go in factory/.env (same keys, NEVER committed — gitignored).
# Phases 2/3 (LLM merge/judge/topic refine) are fail-closed: missing keys => clean stop, no partial writes.
# Free-tier research only. Never reuse the bot runtime key (AI_API_KEY) for factory runs.
#
# Roles (verified from v13 artifacts):
# - OPENCODE_ZEN_API_KEY .... OpenCode Zen (https://opencode.ai/zen/v1) — muse-spark-1.3 primary + spark-1.2/ling/mimo/nemotron fallback
# - OPENCODE_ZEN_API_KEY_2 ... second Zen key for quota rotation (optional;
#   empty = single-key mode). Rotation stops with a clear message when all
#   keys 429 so the operator can switch servers; never committed.
# - OPENROUTER_API_KEY ...... OpenRouter — :free models fallback
# - GOOGLE_AI_API_KEY ....... Google direct (generativelanguage, X-goog-api-key header) — gemini flash-lite fallback
# - AVALAI_API_KEY .......... AvalAI (https://api.avalai.ir/v1, OpenAI-compatible) — paid chain:
#   S2 judge glm-5.3-flash (wired); S3 gemini-3.5-flash-lite + repair gemini-3.8-flash (follow-up).
#   Locked 2026-09-06 (model-chain-lock). Never committed.

OPENCODE_ZEN_API_KEY=
OPENCODE_ZEN_API_KEY_2=
OPENROUTER_API_KEY=
GOOGLE_AI_API_KEY=
AVALAI_API_KEY=
