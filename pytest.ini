[pytest]
testpaths = tests
norecursedirs = tools* build dist .* venv .venv __pycache__ .pytest_cache .ruff_cache backups out tts_cache junit graphify-out
markers =
    research: R&D-only tests (no runtime importers); excluded from default runs, opt back with HAMZABAN_INCLUDE_RESEARCH=1
