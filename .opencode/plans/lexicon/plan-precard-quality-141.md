---
name: plan-precard-quality-141
description: کیفیت به‌ازای حس روی خط 1.4.1 فقط از دیتاست (لینک حس قفل CEFR و مترادف و ادغام را باز می‌کند؛ مثال مال خود حس؛ موضوع صادقانه)
created: 2026-09-14
base_commit: b007ed1
branch: TBD (after identity plan lands)
status: in-progress
---
STATE: phase 0/6 — status:awaiting-owner — focus: قفل ترتیب فازها و قانون حس برند، بعد اجرا. هنوز هیچ کدی دست نخورده.

## اصل (با مالک قفل شده ۲۰۲۶-۰۹-۱۴)

بدون مدرک در سطح حس، چیزی الصاق نمی‌شود. خالیِ صادقانه بهتر از پرِ غلط است. حدس مدل زبانی برای CEFR ممنوع. این پلن روی پلن هویت سوار می‌شود (فازها فرض می‌کنند factory/precard هست؛ فقط spike ممکن است روی درخت فعلی بدود).

## فازها (هر فاز: تست اول، بعد کد، بعد اعتبارسنجی کامل، بعد داور مستقل، بعد کامیت)

| # | کار | مدرک تمام شدن |
|---|---|---|
| P0 | شناسایی فقط-خواندنی: آیا WordNet (تعریف و اعضای سین‌ست) در محیط پایپلاین در دسترس است؟ پوشش زیرمجموعه EVP و جدول sensekey روی نمونه ۲۸۴تایی چقدر است؟ SemCor reachable است؟ برای هر قلم GO یا NO-GO | عددها در یادداشت پلن، بدون کد |
| P1 | لینک حس: حس منتخب Kaikki به سین‌ست WordNet وصل می‌شود و مدرکش (هم‌پوشانی تعریف و guideword) روی ردیف ذخیره می‌شود. لیست طلایی probeها (interest با B1 و C1، حس‌های outside، call) به‌عنوان رگرسیون CI قفل می‌شود | تست‌های probe سبزند |
| P2 | الصاق مترادف و متضاد فقط با لینک مدرک‌دار، وگرنه خالی + provenance روی ردیف. متضادهای خام Kaikki هم خوانده می‌شوند (کم‌اند ولی مفت‌اند) | فیلدها به‌ازای حس‌اند و هیچ ردیفی زوری پر نشده |
| P3 | مثال با قرارداد دقیق: از مثال‌های خود حس منتخب یک تا سه‌تا برمی‌داریم (هر مثال ۵ تا ۲۰ کلمه). حسِ بی‌مثال خالی می‌ماند با پرچم synthetic. مالش lemma و tatoeba از enrich حذف می‌شود و provenance ساده می‌شود به sense و synthetic-needed | ردیف‌های outside متمایزند (خانه و stern و ساختمان) |
| P4 | صداقت CEFR: مهر pool برداشته می‌شود و به‌جایش unmapped می‌نشیند. pool_level همان سیگنال سطح لماست و سر جایش می‌ماند. بزرگ کردن بسته EVP تیکت جدای موکول می‌گیرد | سهم مهرخورده از ۸۳٪ به حدود صفر می‌رسد |
| P5 | موضوعات: guard به رجیستری واحد ۱۶تایی وصل می‌شود (باگ زنده برچسب نامعتبر را می‌بندد)، به دستور داور یک خط اضافه می‌شود که انتخاب‌ها باید معنای clearly متفاوت داشته باشند، قانون حس برند و محصول (کلاس Playboy شماره ۱: دراپ یا آخر صف — مالک قفل می‌کند)، یادداشت tie-break برای leisure در برابر sport | خروجی guard همیشه عضو رجیستری است (تست دارد) و Playboy شماره ۱ رفته یا آخر است |
| P6 | ritual اثبات: ران ۲۸۴ کلمه‌ای + دیف با v141 + prompt جمنای + ارزیابی | دایرکتوری proof و فایل prompt |

ترتیب: P0 بعد P1 بعد P2 و P3 و P4 با هر ترتیبی بعد P5 بعد P6. لینک P1 پیش‌نیاز محتوای P2 و P4 است. P3 و P5 به P1 نیاز ندارند.

## تصمیم‌ها — قفل‌شده با مالک ۲۰۲۶-۰۹-۱۴ (پیشنهاددهنده: عامل، تأییدکننده: مالک)

- D-brand: دراپ حس برند و محصول با تعریف مضیق (موضوع حس برند یا محصول یا شرکت باشد). trade-off ثبت‌شده: اگر تعریف اشتباه خوانده شود یک حس واقعی می‌سوزد ولی با reason ساخت‌یافته و قابل برگشت با rekey. رد شده: آخر-صف (نویز در خروجی می‌ماند).
- D-semcor: ترتیب نسبی حس‌ها از متن‌های واقعی برچسب‌خورده، فقط اگر دیتایش در spike دم دست بود؛ وگرنه بی‌صدا کنار می‌رود و دور دومی لازم نیست. trade-off ثبت‌شده: ترتیب نسبی می‌دهد ولی سطح مطلق نه (آن با جدول sensekey است). رد شده: رد کلی.
- D-order: ترتیب فازها (P0 بعد P1 بعد P2 و P3 و P4 با هر ترتیب بعد P5 بعد P6) تأیید شد چون P1 قفل بقیه را باز می‌کند و P6 آخر است. GATE STATUS = LOCKED.

## معیار قبول (کل پلن)

- شناسه تکراری صفر.
- مثال خالی فقط با پرچم synthetic است.
- هر برچسب guard عضو رجیستری زنده است.
- probeهای طلایی سبزند.
- دیف اثبات نشان می‌دهد مثال و CEFR و مترادف به‌ازای حس متمایزند (outside و interest و call و playboy و working).
