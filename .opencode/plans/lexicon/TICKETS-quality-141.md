---
name: TICKETS-quality-141
description: تیکت‌های فازی پلن کیفیت (اسکلت دقیق، تفصیل فایل‌به‌فایل بعد از spike و بعد از هویت)
created: 2026-09-14
status: in-progress
---
STATE: phase skeletons locked 2026-09-14 — status:awaiting-execution — focus: P0 first; expand P1..P5 to file-level after P0 findings + identity T4

## چرا اسکلت و نه جزئیات (ثبت تصمیم روشی)

تیکت فایل‌به‌فایل امروز حدس می‌شود چون به دو چیز گره خورده که هنوز نیست:
یافته‌های spike (دسترسی WordNet؟ پوشش EVP و sensekey؟ SemCor؟) و خانه جدید
(مسیر فایل‌ها بعد از T4 هویت عوض می‌شود). پس هر فاز زیر با لبه و دامنه
مسئولیتی و معیار قبول قفل است و دروازه REFINE بعد از P0 آن را به تیکت
فایل‌به‌فایل می‌شکافد. نام ماژول‌ها اسمی‌اند (judge و topics و enrich در
خانه factory/precard) و بعد از T4 قطعی می‌شوند.

## تیکت‌ها

### Q0 — spike فقط-خواندنی (بدون کد)
- دامنه: دسترسی سین‌ست WordNet (تعریف و اعضا) در محیط پایپلاین؛ پوشش
  زیرمجموعه EVP و جدول sensekey روی نمونه ۲۸۴تایی؛ reachable بودن SemCor؛
  خواندن مکانیک CEFR-reweight از #۶۹۵ (Q-cefrw).
- لبه: هیچی. معیار قبول: برای هر قلم یک عدد و یک GO یا NO-GO در یادداشت
  پلن. خروجی‌اش ورودی دروازه REFINE است.

### Q1 — لینک حس (قلب پلن)
- دامنه: ماژول judge/enrich در خانه جدید؛ تابع لینک حس Kaikki به سین‌ست با
  مدرک هم‌پوشانی تعریف و guideword؛ ذخیره مدرک روی ردیف؛ لیست طلایی probe
  (interest با B1 و C1، حس‌های outside، call) به‌عنوان رگرسیون CI.
- لبه: Q0 (GO بودن WordNet) و T4 هویت. معیار قبول: probeها سبزند و هر لینک
  مدرک قابل ممیزی دارد.

### Q2 — مترادف و متضاد مدرک‌دار
- دامنه: همان enrich؛ الصاق lemmaهای سین‌ست و antonymها فقط با لینک Q1،
  وگرنه خالی + provenance؛ خواندن متضادهای خام Kaikki (مفت ولی کم).
- لبه: Q1. معیار قبول: هیچ ردیفی زوری پر نشده (تست: attach بدون لینک
  ممنوع است).

### Q3 — مثال دقیق (قرارداد قفل‌شده)
- دامنه: همان enrich؛ از مثال‌های خود حس یک تا سه‌تا (هر مثال ۵ تا ۲۰
  کلمه)؛ حس بی‌مثال خالی با پرچم synthetic؛ حذف مالش lemma و tatoeba؛
  provenance ساده به sense و synthetic-needed؛ سقف جدید PRECARD_MAX_EXAMPLES.
- لبه: هیچی (مستقل از Q1). معیار قبول: ردیف‌های outside متمایزند
  (خانه و stern و ساختمان) و هیچ دو ردیفی مثال یکسان ندارند مگر تعریف
  یکسان که collapse شده.

### Q4 — صداقت CEFR
- دامنه: همان enrich؛ برداشتن مهر pool (به‌جایش unmapped)؛ pool_level
  می‌ماند؛ رشد بسته EVP تیکت جدای موکول.
- لبه: Q1 برای محتوای لینک‌دار (حذف مهر مستقل است). معیار قبول: سهم
  مهرخورده از ۸۳٪ به حدود صفر و هر سطح حس مدرک لینک یا EVP دارد.

### Q5 — موضوعات
- دامنه: ماژول topics؛ guard به رجیستری واحد ۱۶تایی (فیکس باگ زنده)؛ یک
  خط distinctness در دستور داور؛ قانون برند D-brand (دراپ مضیق)؛ یادداشت
  tie-break برای leisure در برابر sport (شاهد: Playboy).
- لبه: T4 هویت برای رجیستری (یا فریز ۱۶تایی اگر هویت عقب افتاد — تصمیم
  اجرا). معیار قبول: خروجی guard همیشه عضو رجیستری است و Playboy شماره ۱
  رفته است.

### Q6 — ritual اثبات
- دامنه: ران ۲۸۴ کلمه‌ای روی خط جدید + دیف با v141 + prompt جمنای +
  ارزیابی؛ همان ritual که برای v141 رفت.
- لبه: Q1..Q5. معیار قبول: دایرکتوری proof و فایل prompt و گزارش ارزیابی.

### REFINE — دروازه تفصیل (بعد از Q0 و T4 هویت)
- دامنه: شکستن Q1..Q5 به تیکت فایل‌به‌فایل با نام واقعی ماژول‌ها و توابع.
- لبه: یافته‌های Q0 + خانه ساخته‌شده. معیار قبول: هر تیکت فایل دقیق
  نام می‌برد و هیچ «TBD»ای نمانده.

## تصمیم‌های قفل‌شده (از پلن کیفیت — تکرار برای چسبیدن به تیکت)

- D-brand: دراپ مضیق. D-semcor: مشروط به دم‌دست بودن. D-order: ترتیب تأییدشده.
  GATE STATUS = LOCKED.
