# CONTEXT v14 — 2026-09-03 — یافته‌های قفل‌شده (anti-compaction-loss)

## زنجیره (همه واقعی، بدون تقلب)
- v14a قطعی (صفر تماس): 500 لم، 4093 uniq، 2101 کارت؛ Other 89.1% (evp-domain 225 صادقانه + keyword 5)؛ flat top-1 آپارتمان؛ سنگ با نگهبان EVP به uniq برگشت (#12) ولی top-4 نشد؛ 15 ادغام عین‌هم با merged_from؛ pos=name صفر.
- v14b ادغام مدل (63 تماس spark-1.3): 2101 ← 1676 کارت، 367 ادغام، خرابی ۰.
- v14c داور+موضوع (63 تماس spark-1.3، جمع 126): judge 494 لم + deterministic ۶ لم (light/chairman/cycling/exchange/stool/drive)؛ Other 27.0% (453/1676)؛ per-CEFR 265/258/298/296/309/250.
- پروب‌ها: rock مبتدی #23 تکان (موسیقی فقط پیشرفته)؛ flat مبتدی #40/#14؛ light fallback [#62 لامپ، #7 سبک]؛ pass مبتدی [#8 گذراندن، #25 بلیط]. «امتحان» در داده نیست (فقط v9 تزریقی داشت).

## یافته‌های ماندگار
1. ادعای keyword-831/thr-0.50 در v13c از کد اعلام‌شده بازتولید نشد (8/400 بالای 0.50). موضوع قطعی فقط evp-domain است.
2. وزن خالص سقف دارد: recall بدون بوست رتبه، سنگ را top-4 نکرد. درس: نگهبان + بوست.
3. مثال فقط بازمانده + فیلتر LLM (communicate#5 با مثال اتاق‌ها؛ flat#32 آبجو با مثال آپارتمان در v13c).
4. Transport Zen: مدل‌ها بدون پیشوند opencode/ (وگرنه 401)؛ هدر UA مرورگر (وگرنه Cloudflare 403)؛ base https://opencode.ai/zen/v1/responses؛ reasoning minimal. همه 126 تماس spark-1.3، fallback لازم نشد.
5. WN count همان SemCor است (استاندارد MFS)؛ شمارش صفر = ترتیب دل‌خواه (مشکل یخ/سنگ) → fallback توزیعی (تاتوبا).
6. کلیدها: factory/.env (gitignore) با OPENCODE_ZEN_API_KEY / OPENROUTER_API_KEY / GOOGLE_AI_API_KEY؛ لودر factory/env_loader.py (fail-closed)؛ قالب factory/.env.example؛ هرگز کامیت نشود.

## قرارداد قفل v14: R1 وزن جدید / R2 نگهبان EVP / R3 هر دو ادغام / R3b سین‌ست واحد + مثال بازمانده / R4 داور 2/3/4 / R5 پک en + README / R6 کلیدها / R7 مرحله‌ای. GATE LOCKED.

## v15 (قفل، در اجرا): بردار موضوع وزن‌دار — هر حس تا ۳ برچسب با وزن (جمع ۱.۰)، spark-1.3 زنجیره، بچ ۸ لم (~63 تماس)، fallback = برچسب فعلی با وزن ۱.۰. خروجی topic_vectors-v15.json. اعتبارسنجی: جمع وزن‌ها، برچسب ۱..۱۳، تطابق id.
