# HANDOFF — paste after compaction (2026-09-05, Tehran)

## Where we are
- Branch `feat/lexicon-card-pilot` (worktree `.worktrees/feat-lexicon-card-pilot`, base now rebased on origin/main ~#569). HEAD has v12 + fixes. PR #564 is STALE (body describes v3) — needs refresh or close + new PR. Docs branch `docs/awl-refresh` open (status HTML + reports).
- Merged to main: #553 (sampling), #557 (shape filter), #560 (AWL), #562 (F4 phrase pool 500 + judge). Live pool: 3000 words + 500 phrases (234 applied kept, 266 dropped).
- Pilot galleries on W:/hamzaban_data_factory/reports/: card-pilot-v11-30 (18/23), card-pilot-v12-30 (18/23, current), card-pilot-v10-20 (13, old sample), pilot-v6-v7-compare, v10-sense-check.md. Status: status-10k-2026-09-04.html (living doc, record cells).
- Locked rules R1-R45 (v1..v12). Latest: R43 context-aware متوسط, R44 superlative→base merge, R45 gallery stepper, R41b LLM sense micro-pass (tri-state), R42 cloze 4-gates, R38 decay, R34 xref, R35 level zipf floors 3.0/2.5/1.5, R28 delta I/O, R27 telemetry, R24 key rotation + STOP (no long backoff — owner rule: 3×429 = stop + report for VPN switch).
- Infra: 2 Zen keys in factory/.env (gitignored, + backup postmerge-562); BATCH 32, SLEEP 3.0; chain spark-1.3→1.2 (+fallbacks in card gen).

## Awaiting owner review
1. Read v12 gallery (18/23 + reasons). Key Qs: kiss/Note fixed? MF متوسط OK? PR #564: refresh or close+new?
2. 500s transient (good/log/back and forth), everyday-use dimension (arcane magic), word-side proper-noun flag, dual-sense card, terms track — all PARKED.

## Awaiting Gemini
- v12 re-score (v11 got 9/10). Open Qs sent: superlative redirect (done), everyday-use, dual-sense.

## Unpersisted discussions (NOT yet tickets/code)
- Coverage heatmap → FILED as issue #590 (open).
- Tier3 60-20-20 + quarantine: assessed — generate_tier3_node is a STUB (returns None), so greenfield design, no modification. Draft rules T1-T3 + F1-F3 (#588) ready for lock, NOT locked.
- #588 F1/F2: PR #591 implemented WITHOUT contract → CLOSED by owner (no merge). Evidence stays in #588. F1-F3 await contract lock.
- Query cache #550: has new consolidated findings comment (v11 evidence); R11 (no pre-cards) is OUTDATED by factory pre-card index — needs rewrite. opencode.yaml fixed to stop eager PRs.
- Dual-sense cards, terms track, word proper-noun flag: parked ideas, no tickets.
- Status HTML needs v12 rows (last update: v6/v7).

## Latest Gemini feedback (v12 gallery)
- Score trajectory 5 (v9) → 8 (v10) → 9 (v11). Approved: acceptance math, CJK allowlist approach, stage names, stepper, bottom-fixed mobile nav.
- Confirmed: R43 context window rule, best→good redirect (anti-fragmentation), invalid chips (لغوشده/رد در گیت), 4-step stepper, debug details.
- Asked for: second-topic-weight render fix + gallery re-run via resume (DONE: weights render, e.g. Society 0.30).

## Next factory steps (in order)
1. Owner reads v12 gallery → verdicts on open Qs.
2. Decide PR #564 (refresh/close+new).
3. Lock Tier3+F1-F3 contract → implement (bot changes!).
4. Build coverage heatmap (#590).
5. Rewrite #550 R11 for pre-card index era.
6. Scale: 3000-word pipeline run (frozen gates) — needs owner go + VPN quota.
