# Handoff — بعد از فشرده‌سازی: طراحی v14 (paste کن)

فشرده‌سازی انجام شد. از اینجا ادامه بده:

## کجاییم (همه واقعی، بدون تقلب)
- v13c نهایی: 500 لم، 2072 کارت، Other 10.5% (218) — با 1023 تماس API واقعی (865 spark-1.3 + 158 ling). فایل‌ها: `factory/fixtures/ranked_senses-v13c.json` + `topic_labels-v13c.json` (کپی در `W:\hamzaban_data_factory\fixtures\`).
- v13b = keep-more قطعی بدون LLM (1241 Other). v13a = وزن 0.30 بدون LLM. گزارش‌های تقلبی قدیمی پاک/اصلاح شدن (بکاپ `*.bak-fake`).
- مدل‌های کارا: spark-1.3 فقط via `/responses` با reasoning minimal (بچ 18، ~13 ثانیه، صفر rate limit در 100+ تماس). کلیدها در سشن قبلی ست بود — دوباره از مالک بگیر.
- گزارش‌ها: `docs/research/lexicon/v13-final-real-report-2026-09-03.html` (مقایسه v6/v9/v10/v12C/v13c) + `CONTEXT-v13-final-2026-09-03.md` (یافته‌ها).
- W مرتب است: `W:\hamzaban_data_factory\README.md` بخوان.

## ضعف‌های کنونی (با ردیف واقعی در گزارش)
1. rock → «یخ» اول است نه «سنگ» (Zipf 0.50 می‌چربد).
2. flat → «جعبه آبجو» اول است نه «آپارتمان» (بدترین عقب‌گرد).
3. Duplicate واقعی: rock#50 و rock#24 عین هم نگه داشته شدند (dedup 0.82 کور بود).
4. موضوع حل شده (10.5%) — دست نزن.

## حالا چی می‌خوام
از تیم طراحی (subagents موازی) طرح‌های بهینه برای **v14** بگیر که دقیقاً همین 3 ضعف را **بدون تقلب** (نه whitelist، نه placeholder، نه +0.4 دستی) با ابزارهای موجود (keyword، Zipf، WN، EVP، Tatoeba، IPA، spark-1.3/minimal) رفع کنن:
- طرح وزن: جریمه اسم خاص/اسلنگ (قانون سراسری)، بسامد per-sense، بوست تطابق دقیق سطح.
- طرح ادغام: muse بچ 20 تایی حس‌های هم‌معنی را merge + نماینده انتخاب کند، بعد judge نهایی per-level.
- طرح استاندارد چندزبانه: خطی که برای DE/TR/HE هم کار کند (فقط pack عوض شود).
هر طرح: تغییر دقیق سلول/فایل + اثر انتظاری روی rock/flat/pass با عدد. بعد بهترین‌ها را ترکیب کن و v14 را مثل v13c واقعی اجرا کن (tqdm زنده + progress JSON + resume + sleep، بدون pipe کردن خروجی).
