# HANDOFF — paste after compaction (2026-09-04, ~15:00 Tehran)

## State
- Merged to main: PR #553 (sampling pipeline: downloader, offset index, seeded sampler), #557 (R5 shape filter + clean 3000-row pool), #560 (AWL coverage + vowel allowlist + regen). HEAD = 724ba77.
- OPEN branch: docs/awl-refresh (status HTML + AWL report; merge or keep for future reports — owner decides).
- Living status doc (update in place, never fork): docs/research/lexicon/status-10k-2026-09-04.html, mirrored at W:\hamzaban_data_factory\reports\. Convention: every row has a record cell (Tehran datetime + PR/commit/issue link + event); changelog table on top, newest first.
- Pool: factory/packs/en/lemmas_10k.csv — 3000 rows / 3000 unique keys, exact mix 364/485/667/727/454/303, pilot overlap intact. 302k multiword candidates counted for F4.
- Locked: R1-R5 (plan-10k-sampling.md) + vowel allowlist amendment (pack-hit OR zipf>3.0) + create-action price 3 (plan quotas open = R12).
- Parked: F4 phrase pool + level rule (NEXT), F5 essential-word guarantee (after label-quality proven), Tier3 5-axis contract (discussion only), R12 plan quotas, exam lists beyond WordLevel TOEFL-1k (pool overlap 9.3% = top-up signal), issue #550 (query cache, blocked on factory).
- AWL: 569 families on W: (raw/awl_families.json); pool covers 178/569 (31%).
- try/fly/sky absent from pool by sampling lottery (not gate) — do NOT rig without owner order.
- Registry/.env live ONLY in W:\hamzaban_data_factory\backups\<ts>-postmerge-*\ (RESTRICTED .env, never print/commit); restore to factory/ in each new worktree. Big data only on W: (raw/ 3.2GB dump + index + lookup, fixtures, reports).
- Primary repo is dirty (other sessions) → read-only, never touch. All work in .worktrees/<branch> from origin/main. Claims file: only docs/help — no seam overlap for factory work.
- Process: contract-lock-gate before any code change; numbered rules, owner picks each; hamzaban-reviewer after each ticket (0 findings or fix+reverify); full validation for .py; owner merges squash; agent merges only on explicit "merge it".
- Infra: opencode review bot lacked write (now fixed per owner); action pinned @latest can change behavior — if review job fails on permissions again, check action version first, not our code.

## Resume
1. Verify primary on main (read-only), create worktree from origin/main, restore registry.db + .env from latest postmerge backup.
2. Next up: F4 phrase pool (needs level-rule proposal first — mini contract), then card-gen pilot for label quality.
3. Keep status HTML updated per the record-cell convention after every step.
